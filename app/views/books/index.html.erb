<% if flash[:notice] %>
 <div class="flash"><%= flash[:notice] %></div>
 <% end %>

  <h1>Books</h1>

  <table>
   <thead>
　  <tr>
     <th>Title</th>
     <th>Body</th>
     <th colspan="3"></th>
    </tr>
   </thead>
    <tbody>
     <% @books.each do |book| %>
      <tr>
       <td><%= book.title %></td>
       <td><%= book.body %></tb>
       <td><%= link_to 'Show', book_path(book) %></td>
			　　　<td><%= link_to 'Edit', edit_book_path(book) %></td>
　　　 <td><%= link_to 'Destroy', book_path(book), method: :delete, "data-confirm" => "Are you sure？" %></tb>
      </tr>
     <% end %>
    </tbody>
  </table>


 <h2>New book</h2>

  <%= form_with model: @book, url:'/books', local:true do |f| %>
  <!--errorメッセージ-->
  <% if @book.errors.any? %>
   <div class="alert-warning">
     <ul>
     <p>error</p>
       <% @book.errors.full_messages.each do |message| %>
         <li><%= message %></li>
       <% end %>
     </ul>
   </div>
  <% end %>
  <!--ここまでerrorメッセージ-->


     <h5>Tilte</h5>
     <%= f.text_field :title %>
     <h5>Body</h5>
     <%= f.text_area :body %><br>
     <%= f.submit 'Create Book' %>
  <% end %>